<div>
  <h1 class="font-bold text-4xl text-blue-500">Welcome, <%= current_user.first_name + " " + current_user.last_name %> </h1>
  <% if current_user.doctor? %>
    <p> <%= Category.find(current_user.category_id).title.delete_suffix("s") %> </p>
  <% end %>
</div>